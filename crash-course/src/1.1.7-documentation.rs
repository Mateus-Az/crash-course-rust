/*
para documentar nossa aplicação utilizamos 3-barras ///
comando para gerar documentação: cargo doc --open
rustup doc -> com esse comando podemos ler a documentação localmente, muito bem organizada e clean
*/
///Enum de cores
enum Color {
    Green,
    Red,
}
